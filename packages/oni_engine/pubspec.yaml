name: oni_engine
description: Pure-Dart engine that models and solves Oxygen Not Included production pipelines.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
dependencies:
  archive: ^4.2.0
